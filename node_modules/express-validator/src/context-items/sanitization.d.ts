import { Context } from 'express-validator/src/context';
import { CustomSanitizer, Meta, StandardSanitizer } from 'express-validator/src/base';
import { ContextItem } from 'express-validator/src/context-items/context-item';
export declare class Sanitization implements ContextItem {
    private readonly sanitizer;
    private readonly custom;
    private readonly options;
    constructor(sanitizer: StandardSanitizer | CustomSanitizer, custom: boolean, options?: any[]);
    run(context: Context, value: any, meta: Meta): Promise<void>;
}
