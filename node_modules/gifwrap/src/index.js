'use strict';

const BitmapImage = require('gifwrap/src/bitmapimage');
const { Gif, GifError } = require('gifwrap/src/gif');
const { GifCodec } = require('gifwrap/src/gifcodec');
const { GifFrame } = require('gifwrap/src/gifframe');
const GifUtil = require('gifwrap/src/gifutil');

module.exports = {
    BitmapImage,
    Gif,
    GifCodec,
    GifFrame,
    GifUtil,
    GifError
};
