var testDocument = require("min-document/test/test-document")
var testDomElement = require("min-document/test/test-dom-element")
var testDomComment = require("min-document/test/test-dom-comment")
var document = require("min-document")

testDocument(document)
testDomElement(document)
testDomComment(document)

if (typeof window !== "undefined" && window.document) {
    testDocument(window.document)
    testDomElement(window.document)
    testDomComment(window.document)
}
